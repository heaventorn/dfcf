# 东方财富爬虫 · 部署说明

本项目为「大盘与泡沫报告」爬虫：自动采集当日 A 股大盘 + 红利 ETF 追踪 + 罗力豪空中飞人指数（泡沫爆破风险），生成一份综合 HTML 报告。

以下为在**另一台 PC** 上部署的完整步骤（约 10 分钟）。

## 1. 环境要求

- Windows / macOS / Linux
- Python 3.9+（推荐 3.10~3.13）
- 需联网（东方财富 / FRED / 腾讯 / 新浪）

## 2. 安装依赖

```bash
pip install requests pandas playwright
```

## 3. 安装浏览器（国内镜像，速度快）

**Windows PowerShell：**

```powershell
$env:PLAYWRIGHT_DOWNLOAD_HOST="https://npmmirror.com/mirrors/playwright/"
python -m playwright install chromium
```

**macOS / Linux：**

```bash
PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright/ python -m playwright install chromium
```

> 若下载仍慢或失败，可改用 `python -m playwright install chromium --with-deps`（Linux），或多次重试。

## 4. 首次运行（登录）

```bash
python main.py
```

首次会弹出浏览器窗口，用「东方财富 App」扫码登录（登录页有二维码）。登录成功后 Cookie 保存在本机 `cookies.json`，之后运行自动复用，无需重复扫码。

> 行情数据本身无需登录；登录主要用于后续可能的个性化功能。若不想登录，可始终用 `--no-login`。

## 5. 日常运行

```bash
python main.py             # 自动登录 + 采集 + 生成报告
python main.py --no-login  # 跳过登录，直接用公开接口
```

Windows 下也可**双击 `启动爬虫.bat`** 运行。

## 6. 输出

| 文件 | 说明 |
| --- | --- |
| `output/daily_report_*.html` | 综合图文报告（大盘 + 红利 + 空中飞人指数），浏览器打开 |
| `output/latest_market.json` | 当日原始数据（结构化，方便二次处理） |

## 7. 注意事项

- **`cookies.json` 在本机生成**：新机器首次运行需重新扫码登录，请勿把旧机器的 `cookies.json` 拷贝过去（会导致登录态异常）。
- 空中飞人指数的全球宏观数据（美债、VIX、美元、黄金、原油、汇率）来自 FRED / 腾讯 / 新浪，需联网且不消耗任何 API 额度。
- 所有数据源已内置多源自动切换与冷却恢复，单源被限流时自动换源，无需干预。
- 各功能与评分卡细节详见 `README.md`。

## 8. 文件说明

```
eastmoney_crawler/
├── main.py            # 主入口
├── login.py           # 登录模块（扫码 / 手动 Cookie）
├── sources.py         # 多数据源适配层（异常自动切换）
├── collector.py       # 大盘数据采集
├── dividend.py        # 红利 ETF 追踪分析
├── airman.py          # 空中飞人指数（固定评分标准，全自动）
├── html_report.py     # HTML 报告生成
├── config.py          # 配置
├── test_sources.py    # 数据源冒烟测试（python test_sources.py）
└── 启动爬虫.bat        # Windows 双击运行
```
